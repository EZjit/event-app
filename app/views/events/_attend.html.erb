<%= form_with model: current_user.attends.build(event_id: event.id, attendee_id: current_user.id) do |f| %>
  <%= f.hidden_field :event_id %>
  <%= f.hidden_field :attendee_id, value: current_user.id %>
  <%= f.submit 'Attend to event!', class: 'btn btn-success mt-2 mb-2' %>
<% end %>